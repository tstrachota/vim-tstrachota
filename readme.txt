Tomas Strachota - personal vim settings

Unix setup:
1. clone the repo into your desired <REPO_LOCATION>
2. setup the paths:
    ln -s ~/.vim   <REPO_LOCATION>
    ln -s ~/.vimrc <REPO_LOCATION>/_vimrc


